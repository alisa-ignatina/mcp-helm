# Default values for mcp-gitlab-helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: iwakitakuma/gitlab-mcp
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "2.0.7"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 3002

# Health check probes configuration
livenessProbe:
  enabled: true
  initialDelaySeconds: 60
  periodSeconds: 30
  timeoutSeconds: 10
  failureThreshold: 3

readinessProbe:
  enabled: true
  initialDelaySeconds: 15
  periodSeconds: 10
  timeoutSeconds: 10
  failureThreshold: 3

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  path: /
  pathType: Prefix
  hosts:
    - gitlab-mcp.local
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

# Environment variables for the MCP server
env: {}
  # GITLAB_TOKEN: "your-gitlab-token"
  # GITLAB_URL: "https://gitlab.com"

# Environment variables from secrets
envSecrets: {}

# Secret configuration for sensitive data
secretEnv:
  data: {}
    # GITLAB_TOKEN: "your-gitlab-token-base64-encoded"

# Additional sidecar containers rendered verbatim into the pod (template
# expressions are evaluated). Each entry accepts any fields allowed in the
# Kubernetes `Container` spec such as `command`, `args`, `env`, `ports`,
# or `volumeMounts`.
# Example:
# sidecars:
#   - name: sidecar
#     image: busybox
#     command: ["sh", "-c", "echo sidecar && sleep 3600"]
sidecars: []

# Static /etc/hosts entries for the pod — useful when the GitLab hostname
# is only resolvable by a DNS server the cluster cannot reach (e.g. behind
# a VPN reached via a sidecar).
# Example:
# hostAliases:
#   - ip: 10.0.0.5
#     hostnames:
#       - gitlab.internal.example.com
hostAliases: []
